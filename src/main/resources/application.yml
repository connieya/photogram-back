server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
      
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB

  security:
    user:
      name: test
      password: 1234
    oauth2:
      client:
        registration:
          facebook:
            client-id: 908264090410986
            client-secret: 024c81a22d42bd36d623f06b7b5e887e
            scope:
              - public-profile
              - email
# S3
cloud:
  aws:
    s3:
      bucket: ${bucket}
    credentials:
      access-key: ${accessKey}
      secret-key: ${secretKey}
    region:
      static: ${region}
    stack:
      auto: false




file:
  path: C:/workspace/photogram-front/public/images/


jwt:
  header: Authorization
  secret: ${jwtSecretKey}
